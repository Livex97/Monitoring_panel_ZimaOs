#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to secrets.h and enter your actual local Wi-Fi and ZimaOS host details.
// secrets.h is listed in .gitignore and must NOT be committed to version control.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#define ZIMAOS_MONITOR_HOST "192.168.1.100"
#define ZIMAOS_MONITOR_PORT 8787
#define ZIMAOS_API_KEY "" // Optional API key if set on backend

#endif // SECRETS_H
